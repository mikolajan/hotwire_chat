<% if @new_room.new_record? %>
  <%= turbo_stream.update :new_room_errors, partial: 'shared/errors', locals: { resource: @new_room } %>
<% else %>
  <%= turbo_stream.update :new_room, '' %>
<% end %>
